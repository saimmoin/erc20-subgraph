{
  "name": "beagle-token-tracker",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.studio.thegraph.com/deploy/ beagle-token-tracker",
    "create-local": "graph create --node http://localhost:8020/ beagle-token-tracker",
    "remove-local": "graph remove --node http://localhost:8020/ beagle-token-tracker",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 beagle-token-tracker",
    "test": "graph test"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.38.0",
    "@graphprotocol/graph-ts": "0.29.1"
  },
  "devDependencies": {
    "matchstick-as": "0.5.0"
  }
}
